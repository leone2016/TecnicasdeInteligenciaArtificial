=== Run information ===

Scheme:       weka.classifiers.rules.Prism 
Relation:     car
Instances:    1728
Attributes:   7
              compra
              maint
              puertas
              personas
              lug_boot
              seguridad
              class
Test mode:    10-fold cross-validation

=== Classifier model (full training set) ===

Prism rules
----------
If personas = 2 then unacc
If seguridad = low then unacc
If compra = vhigh
   and maint = vhigh then unacc
If compra = vhigh
   and maint = high then unacc
If maint = vhigh
   and compra = high then unacc
If lug_boot = small
   and seguridad = med
   and compra = high then unacc
If lug_boot = small
   and puertas = 2
   and personas = more then unacc
If lug_boot = small
   and seguridad = med
   and compra = vhigh then unacc
If maint = vhigh
   and lug_boot = small
   and seguridad = med then unacc
If puertas = 2
   and lug_boot = med
   and seguridad = med
   and compra = high then unacc
If seguridad = med
   and puertas = 2
   and lug_boot = med
   and compra = vhigh then unacc
If seguridad = med
   and maint = vhigh
   and puertas = 2
   and lug_boot = med then unacc
If maint = high
   and compra = med
   and lug_boot = small
   and seguridad = med then unacc
If puertas = 3
   and lug_boot = med
   and personas = 4
   and seguridad = med
   and compra = high then unacc
If lug_boot = med
   and puertas = 3
   and compra = vhigh
   and personas = 4
   and seguridad = med then unacc
If maint = vhigh
   and puertas = 3
   and lug_boot = med
   and personas = 4
   and seguridad = med then unacc
If maint = high
   and compra = med
   and puertas = 2
   and lug_boot = med
   and seguridad = med then unacc
If maint = high
   and puertas = 3
   and compra = med
   and lug_boot = med
   and personas = 4
   and seguridad = med then unacc
If seguridad = high
   and personas = 4
   and compra = high
   and maint = high then acc
If seguridad = high
   and personas = 4
   and compra = high
   and maint = med then acc
If seguridad = high
   and personas = more
   and compra = high
   and maint = high
   and lug_boot = med then acc
If seguridad = high
   and personas = 4
   and compra = med
   and maint = vhigh then acc
If personas = more
   and seguridad = high
   and compra = high
   and maint = med
   and lug_boot = med then acc
If personas = more
   and seguridad = high
   and compra = high
   and maint = low
   and lug_boot = med then acc
If seguridad = med
   and personas = 4
   and lug_boot = big
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = med
   and lug_boot = big
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = high
   and compra = high
   and maint = high
   and lug_boot = big then acc
If seguridad = med
   and personas = 4
   and compra = med
   and maint = med then acc
If personas = more
   and seguridad = med
   and compra = med
   and maint = med
   and lug_boot = med then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = vhigh
   and lug_boot = med then acc
If personas = more
   and seguridad = med
   and lug_boot = big
   and compra = med
   and maint = vhigh then acc
If seguridad = high
   and personas = 4
   and lug_boot = small
   and compra = med
   and maint = high then acc
If personas = more
   and seguridad = high
   and compra = high
   and maint = med
   and lug_boot = big then acc
If personas = more
   and seguridad = med
   and lug_boot = big
   and maint = med
   and compra = vhigh then acc
If seguridad = high
   and personas = 4
   and compra = vhigh
   and maint = med then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = high
   and lug_boot = med then acc
If seguridad = med
   and personas = 4
   and compra = low
   and maint = high then acc
If personas = more
   and seguridad = med
   and compra = low
   and maint = high
   and lug_boot = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = high
   and puertas = 4
   and lug_boot = small
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = high
   and puertas = 5more
   and lug_boot = small
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = med
   and lug_boot = big
   and compra = high
   and maint = med then acc
If personas = more
   and seguridad = high
   and compra = vhigh
   and maint = med
   and lug_boot = med then acc
If personas = more
   and seguridad = med
   and compra = med
   and maint = med
   and lug_boot = big then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = vhigh
   and lug_boot = big then acc
If seguridad = med
   and personas = 4
   and lug_boot = big
   and compra = high
   and maint = med then acc
If personas = more
   and seguridad = med
   and compra = low
   and maint = high
   and lug_boot = big then acc
If seguridad = high
   and personas = 4
   and maint = low
   and compra = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = med
   and maint = vhigh then acc
If seguridad = med
   and personas = 4
   and maint = low
   and lug_boot = small
   and compra = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and compra = med
   and maint = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and compra = med
   and maint = vhigh then acc
If personas = more
   and seguridad = med
   and lug_boot = med
   and compra = med
   and maint = vhigh
   and puertas = 3 then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and maint = med
   and compra = vhigh then acc
If personas = more
   and seguridad = med
   and lug_boot = med
   and maint = med
   and compra = vhigh
   and puertas = 3 then acc
If personas = more
   and seguridad = high
   and maint = low
   and compra = vhigh
   and lug_boot = med then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and puertas = 4
   and compra = high
   and maint = high then acc
If seguridad = med
   and personas = more
   and maint = low
   and compra = vhigh
   and lug_boot = big then acc
If seguridad = med
   and personas = 4
   and lug_boot = big
   and compra = med
   and maint = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and maint = med
   and compra = vhigh then acc
If seguridad = med
   and personas = more
   and compra = med
   and maint = med
   and puertas = 3 then acc
If seguridad = med
   and personas = 4
   and compra = low
   and lug_boot = small
   and maint = med then acc
If personas = more
   and seguridad = high
   and compra = high
   and maint = low
   and lug_boot = big then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and puertas = 5more
   and compra = high
   and maint = high then acc
If seguridad = med
   and personas = more
   and compra = low
   and maint = high
   and puertas = 3 then acc
If personas = 4
   and seguridad = high
   and compra = high
   and maint = low then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and compra = med
   and maint = high
   and puertas = 3 then acc
If seguridad = med
   and personas = more
   and compra = low
   and maint = high
   and puertas = 4 then acc
If seguridad = med
   and personas = 4
   and maint = low
   and lug_boot = med
   and compra = vhigh
   and puertas = 4 then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and maint = med
   and compra = vhigh then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and maint = med
   and compra = high
   and puertas = 3 then acc
If seguridad = med
   and personas = 4
   and lug_boot = big
   and compra = vhigh
   and maint = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = high
   and maint = med then acc
If personas = more
   and seguridad = med
   and compra = med
   and maint = med
   and puertas = 4 then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and compra = high
   and maint = med then acc
If personas = more
   and seguridad = med
   and compra = low
   and maint = vhigh
   and lug_boot = big then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and compra = high
   and maint = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = med
   and maint = high then acc
If seguridad = med
   and personas = 4
   and maint = low
   and lug_boot = big
   and compra = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and compra = med
   and maint = high then acc
If personas = more
   and seguridad = high
   and lug_boot = big
   and compra = vhigh
   and maint = med then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and puertas = 4
   and compra = high
   and maint = med then acc
If seguridad = med
   and personas = more
   and maint = low
   and compra = high
   and lug_boot = big then acc
If personas = 4
   and seguridad = high
   and compra = med
   and maint = high then acc
If seguridad = med
   and personas = 4
   and compra = low
   and maint = vhigh
   and lug_boot = big then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and compra = med
   and maint = high then acc
If personas = more
   and seguridad = high
   and compra = low
   and maint = vhigh
   and lug_boot = med then acc
If seguridad = med
   and personas = more
   and lug_boot = med
   and puertas = 5more
   and compra = high
   and maint = med then acc
If seguridad = med
   and personas = more
   and compra = med
   and puertas = 5more
   and maint = med then acc
If seguridad = med
   and personas = 4
   and maint = low
   and compra = high
   and lug_boot = big then acc
If personas = more
   and seguridad = med
   and lug_boot = med
   and maint = low
   and compra = vhigh
   and puertas = 3 then acc
If personas = more
   and seguridad = med
   and compra = med
   and maint = high
   and lug_boot = big then acc
If compra = low
   and maint = vhigh
   and seguridad = high
   and personas = 4 then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = low
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and puertas = 4
   and compra = med
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and puertas = 5more
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and compra = low
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and puertas = 4
   and compra = high
   and maint = high then acc
If personas = more
   and seguridad = high
   and maint = low
   and compra = vhigh
   and lug_boot = big then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and puertas = 5more
   and compra = med
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and puertas = 5more
   and compra = high
   and maint = med then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and maint = low
   and compra = high
   and puertas = 3 then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and compra = med
   and puertas = 4
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and puertas = 4
   and compra = vhigh
   and maint = med then acc
If seguridad = med
   and compra = low
   and personas = 4
   and maint = low
   and lug_boot = small then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and compra = low
   and maint = vhigh then acc
If personas = more
   and seguridad = med
   and compra = low
   and lug_boot = small
   and maint = med
   and puertas = 3 then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = high
   and lug_boot = big then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and compra = low
   and puertas = 2
   and maint = med then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and puertas = 5more
   and compra = vhigh
   and maint = med then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and puertas = 5more
   and compra = med
   and maint = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and maint = low
   and compra = vhigh
   and puertas = 4 then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and maint = low
   and compra = high
   and puertas = 4 then acc
If personas = more
   and seguridad = med
   and compra = low
   and maint = high
   and puertas = 5more then acc
If personas = more
   and seguridad = med
   and compra = low
   and lug_boot = small
   and maint = low
   and puertas = 3 then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and maint = low
   and compra = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and compra = low
   and maint = vhigh
   and puertas = 3 then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and compra = low
   and puertas = 3
   and maint = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and maint = low
   and compra = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and puertas = 5more
   and compra = vhigh
   and maint = med then acc
If personas = more
   and seguridad = med
   and maint = low
   and puertas = 5more
   and lug_boot = small
   and compra = med then acc
If personas = more
   and seguridad = med
   and lug_boot = med
   and puertas = 5more
   and maint = low
   and compra = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and maint = low
   and compra = vhigh then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and maint = low
   and compra = med
   and puertas = 2 then acc
If personas = more
   and seguridad = med
   and compra = low
   and puertas = 4
   and lug_boot = small
   and maint = med then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3
   and compra = high
   and maint = low then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and compra = med
   and maint = high
   and puertas = 4 then acc
If seguridad = med
   and lug_boot = med
   and personas = 4
   and compra = low
   and maint = vhigh
   and puertas = 4 then acc
If personas = more
   and seguridad = high
   and compra = low
   and maint = vhigh
   and lug_boot = big then acc
If seguridad = med
   and lug_boot = med
   and personas = more
   and compra = low
   and maint = vhigh
   and puertas = 4 then acc
If personas = 4
   and compra = med
   and seguridad = med
   and maint = high
   and lug_boot = big then acc
If seguridad = high
   and personas = 4
   and lug_boot = small
   and compra = med
   and maint = med then acc
If personas = more
   and seguridad = med
   and maint = low
   and lug_boot = med
   and compra = high
   and puertas = 4 then acc
If personas = more
   and seguridad = med
   and compra = low
   and puertas = 5more
   and lug_boot = small
   and maint = med then acc
If personas = more
   and seguridad = med
   and maint = low
   and lug_boot = small
   and compra = med
   and puertas = 3 then acc
If compra = low
   and maint = high
   and seguridad = high
   and lug_boot = small
   and personas = 4 then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and puertas = 5more
   and maint = low
   and compra = vhigh then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 4
   and compra = high
   and maint = low then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and maint = low
   and compra = low
   and puertas = 2 then acc
If personas = more
   and seguridad = med
   and lug_boot = med
   and puertas = 5more
   and compra = high
   and maint = low then acc
If personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 5more
   and compra = high
   and maint = low then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and compra = med
   and maint = high
   and puertas = 4 then acc
If personas = more
   and seguridad = med
   and compra = low
   and lug_boot = med
   and puertas = 2
   and maint = med then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = med
   and lug_boot = small
   and puertas = 3 then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and puertas = 5more
   and compra = high
   and maint = low then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and compra = med
   and puertas = 5more
   and maint = high then acc
If personas = more
   and seguridad = med
   and maint = low
   and compra = low
   and lug_boot = small
   and puertas = 4 then acc
If lug_boot = med
   and seguridad = med
   and personas = more
   and compra = med
   and maint = high
   and puertas = 5more then acc
If lug_boot = med
   and personas = 4
   and seguridad = med
   and compra = low
   and puertas = 3
   and maint = low then acc
If personas = more
   and seguridad = high
   and maint = high
   and compra = low
   and lug_boot = small
   and puertas = 3 then acc
If lug_boot = med
   and personas = 4
   and maint = med
   and compra = med
   and seguridad = high
   and puertas = 2 then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = med
   and lug_boot = small
   and puertas = 4 then acc
If lug_boot = med
   and seguridad = med
   and personas = more
   and compra = low
   and maint = vhigh
   and puertas = 5more then acc
If lug_boot = med
   and personas = 4
   and seguridad = med
   and maint = med
   and puertas = 4
   and compra = vhigh then acc
If personas = more
   and seguridad = high
   and maint = high
   and compra = low
   and lug_boot = small
   and puertas = 4 then acc
If lug_boot = med
   and personas = 4
   and seguridad = high
   and maint = high
   and compra = low
   and puertas = 2 then acc
If personas = more
   and seguridad = med
   and maint = low
   and compra = med
   and lug_boot = small
   and puertas = 4 then acc
If lug_boot = med
   and seguridad = med
   and personas = more
   and maint = low
   and puertas = 2
   and compra = med then acc
If lug_boot = med
   and personas = 4
   and puertas = 3
   and seguridad = high
   and compra = med
   and maint = med then acc
If lug_boot = med
   and seguridad = med
   and personas = 4
   and compra = med
   and maint = low
   and puertas = 3 then acc
If personas = more
   and seguridad = high
   and compra = med
   and maint = med
   and lug_boot = small
   and puertas = 5more then acc
If compra = low
   and maint = high
   and seguridad = high
   and personas = more
   and lug_boot = small
   and puertas = 5more then acc
If lug_boot = med
   and compra = low
   and maint = high
   and seguridad = high
   and puertas = 2
   and personas = more then acc
If lug_boot = med
   and seguridad = med
   and compra = low
   and puertas = 2
   and maint = low
   and personas = more then acc
If lug_boot = med
   and personas = 4
   and compra = low
   and maint = vhigh
   and puertas = 5more
   and seguridad = med then acc
If lug_boot = med
   and maint = med
   and compra = high
   and puertas = 4
   and personas = 4
   and seguridad = med then acc
If personas = more
   and seguridad = med
   and compra = low
   and puertas = 5more
   and lug_boot = small
   and maint = low then acc
If lug_boot = med
   and maint = high
   and puertas = 3
   and compra = high
   and personas = more
   and seguridad = med then acc
If seguridad = high
   and lug_boot = med
   and puertas = 2
   and compra = med
   and maint = med
   and personas = more then acc
If maint = high
   and compra = low
   and puertas = 3
   and personas = 4
   and lug_boot = med
   and seguridad = high then acc
If compra = low
   and maint = med
   and seguridad = med
   and lug_boot = big
   and personas = 4 then good
If maint = low
   and compra = med
   and seguridad = med
   and lug_boot = big
   and personas = 4 then good
If compra = low
   and maint = low
   and seguridad = med
   and lug_boot = big
   and personas = 4 then good
If compra = low
   and maint = med
   and personas = more
   and seguridad = med
   and lug_boot = big then good
If maint = low
   and compra = med
   and personas = more
   and seguridad = med
   and lug_boot = big then good
If compra = low
   and maint = low
   and personas = more
   and seguridad = med
   and lug_boot = big then good
If compra = low
   and maint = med
   and seguridad = high
   and lug_boot = small
   and personas = 4 then good
If maint = low
   and compra = med
   and seguridad = high
   and lug_boot = small
   and personas = 4 then good
If compra = low
   and maint = low
   and seguridad = high
   and lug_boot = small
   and personas = 4 then good
If compra = low
   and maint = med
   and lug_boot = med
   and seguridad = med
   and personas = more
   and puertas = 3 then good
If maint = low
   and compra = med
   and lug_boot = med
   and seguridad = med
   and personas = more
   and puertas = 3 then good
If compra = low
   and maint = low
   and lug_boot = med
   and seguridad = med
   and personas = more
   and puertas = 3 then good
If compra = low
   and maint = med
   and lug_boot = med
   and seguridad = med
   and puertas = 4
   and personas = 4 then good
If maint = low
   and compra = med
   and lug_boot = med
   and seguridad = med
   and puertas = 4
   and personas = 4 then good
If compra = low
   and maint = low
   and lug_boot = med
   and seguridad = med
   and puertas = 4
   and personas = 4 then good
If compra = low
   and maint = med
   and personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3 then good
If maint = low
   and compra = med
   and personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3 then good
If compra = low
   and maint = low
   and personas = more
   and seguridad = high
   and lug_boot = small
   and puertas = 3 then good
If compra = low
   and maint = med
   and lug_boot = med
   and seguridad = med
   and puertas = 5more
   and personas = 4 then good
If maint = low
   and compra = med
   and lug_boot = med
   and seguridad = med
   and puertas = 5more
   and personas = 4 then good
If compra = low
   and maint = low
   and lug_boot = med
   and seguridad = med
   and puertas = 5more
   and personas = 4 then good
If compra = low
   and maint = med
   and personas = more
   and seguridad = med
   and lug_boot = med
   and puertas = 4 then good
If maint = low
   and compra = med
   and personas = more
   and seguridad = med
   and lug_boot = med
   and puertas = 4 then good
If compra = low
   and maint = low
   and personas = more
   and seguridad = med
   and lug_boot = med
   and puertas = 4 then good
If compra = low
   and seguridad = high
   and maint = med
   and lug_boot = small
   and personas = more
   and puertas = 4 then good
If maint = low
   and seguridad = high
   and compra = med
   and lug_boot = small
   and personas = more
   and puertas = 4 then good
If compra = low
   and seguridad = high
   and maint = low
   and lug_boot = small
   and personas = more
   and puertas = 4 then good
If compra = low
   and seguridad = high
   and maint = med
   and lug_boot = med
   and puertas = 2
   and personas = 4 then good
If maint = low
   and seguridad = high
   and compra = med
   and lug_boot = med
   and puertas = 2
   and personas = 4 then good
If compra = low
   and maint = low
   and seguridad = high
   and lug_boot = med
   and puertas = 2
   and personas = 4 then good
If compra = low
   and personas = more
   and puertas = 5more
   and maint = med
   and seguridad = med
   and lug_boot = med then good
If maint = low
   and personas = more
   and puertas = 5more
   and compra = med
   and seguridad = med
   and lug_boot = med then good
If compra = low
   and seguridad = high
   and maint = med
   and personas = more
   and lug_boot = small
   and puertas = 5more then good
If maint = low
   and seguridad = high
   and compra = med
   and personas = more
   and lug_boot = small
   and puertas = 5more then good
If compra = low
   and maint = low
   and personas = more
   and puertas = 5more
   and seguridad = med
   and lug_boot = med then good
If compra = low
   and seguridad = high
   and maint = low
   and personas = more
   and lug_boot = small
   and puertas = 5more then good
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 2
   and personas = more
   and maint = med then good
If maint = low
   and seguridad = high
   and lug_boot = med
   and puertas = 2
   and personas = more
   and compra = med then good
If compra = low
   and seguridad = high
   and maint = low
   and lug_boot = med
   and puertas = 2
   and personas = more then good
If puertas = 3
   and personas = 4
   and seguridad = high
   and compra = low
   and lug_boot = med
   and maint = med then good
If maint = low
   and puertas = 3
   and personas = 4
   and seguridad = high
   and lug_boot = med
   and compra = med then good
If compra = low
   and maint = low
   and puertas = 3
   and personas = 4
   and seguridad = high
   and lug_boot = med then good
If seguridad = high
   and compra = low
   and lug_boot = big
   and personas = 4
   and maint = high then vgood
If seguridad = high
   and compra = low
   and lug_boot = big
   and personas = more
   and maint = high then vgood
If seguridad = high
   and compra = low
   and lug_boot = big
   and maint = med
   and personas = 4 then vgood
If seguridad = high
   and compra = low
   and personas = more
   and lug_boot = big
   and maint = med then vgood
If seguridad = high
   and compra = med
   and maint = med
   and lug_boot = big
   and personas = 4 then vgood
If seguridad = high
   and maint = low
   and compra = med
   and lug_boot = big
   and personas = 4 then vgood
If seguridad = high
   and compra = low
   and maint = low
   and lug_boot = big
   and personas = 4 then vgood
If seguridad = high
   and compra = low
   and personas = more
   and maint = low
   and lug_boot = big then vgood
If seguridad = high
   and compra = med
   and personas = more
   and maint = med
   and lug_boot = big then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and personas = more
   and maint = high
   and puertas = 3 then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and personas = more
   and maint = med
   and puertas = 3 then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and personas = more
   and maint = low
   and puertas = 3 then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 4
   and personas = 4
   and maint = high then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 5more
   and personas = 4
   and maint = high then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and personas = more
   and puertas = 4
   and maint = high then vgood
If seguridad = high
   and compra = med
   and maint = low
   and personas = more
   and lug_boot = big then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 5more
   and personas = more
   and maint = high then vgood
If seguridad = high
   and lug_boot = med
   and compra = med
   and maint = med
   and personas = more
   and puertas = 3 then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 4
   and maint = med
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and compra = med
   and maint = low
   and personas = more
   and puertas = 3 then vgood
If seguridad = high
   and lug_boot = med
   and compra = low
   and puertas = 5more
   and maint = med
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and compra = med
   and maint = med
   and puertas = 4
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and maint = low
   and compra = med
   and puertas = 4
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and maint = low
   and compra = low
   and puertas = 4
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and puertas = 5more
   and compra = med
   and maint = med
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and personas = more
   and compra = low
   and maint = med
   and puertas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and maint = low
   and puertas = 5more
   and compra = med
   and personas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and personas = more
   and compra = low
   and maint = low
   and puertas = 4 then vgood
If seguridad = high
   and lug_boot = med
   and puertas = 5more
   and compra = low
   and maint = low
   and personas = 4 then vgood
If seguridad = high
   and personas = more
   and lug_boot = med
   and compra = med
   and maint = med
   and puertas = 4 then vgood
If seguridad = high
   and puertas = 5more
   and personas = more
   and lug_boot = med
   and compra = low
   and maint = med then vgood
If seguridad = high
   and personas = more
   and compra = med
   and maint = low
   and lug_boot = med
   and puertas = 4 then vgood
If puertas = 5more
   and seguridad = high
   and personas = more
   and lug_boot = med
   and compra = med
   and maint = med then vgood
If maint = low
   and puertas = 5more
   and seguridad = high
   and personas = more
   and lug_boot = med
   and compra = med then vgood
If compra = low
   and maint = low
   and puertas = 5more
   and seguridad = high
   and personas = more
   and lug_boot = med then vgood


Time taken to build model: 0.19 seconds

=== Stratified cross-validation ===
=== Summary ===

Correctly Classified Instances        1543               89.294  %
Incorrectly Classified Instances        61                3.5301 %
Kappa statistic                          0.9039
Mean absolute error                      0.019 
Root mean squared error                  0.1379
Relative absolute error                  9.5015 %
Root relative squared error             45.058  %
UnClassified Instances                 124                7.1759 %
Total Number of Instances             1728     

=== Detailed Accuracy By Class ===

                 TP Rate  FP Rate  Precision  Recall   F-Measure  MCC      ROC Area  PRC Area  Class
                 0,998    0,020    0,993      0,998    0,996      0,983    0,990     0,992     unacc
                 0,925    0,022    0,903      0,925    0,914      0,895    0,842     0,703     acc
                 0,557    0,012    0,642      0,557    0,596      0,583    0,741     0,336     good
                 0,773    0,003    0,872      0,773    0,819      0,816    0,760     0,474     vgood
Weighted Avg.    0,962    0,020    0,960      0,962    0,961      0,947    0,947     0,900     

=== Confusion Matrix ===

    a    b    c    d   <-- classified as
 1204    2    0    0 |    a = unacc
    8  271   11    3 |    b = acc
    0   25   34    2 |    c = good
    0    2    8   34 |    d = vgood

